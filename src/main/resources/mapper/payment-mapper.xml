<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itbank.mall.mapper.PaymentMapper">

    <insert id="insertPayment" parameterType="com.itbank.mall.entity.Payment">
        INSERT INTO payment
        (member_id, order_id, amount, status, payment_method, transaction_id)
        VALUES
        (#{memberId}, #{orderId}, #{amount}, #{status}, #{paymentMethod}, #{transactionId})
    </insert>
    
        <insert id="insert" parameterType="com.itbank.mall.entity.Payment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment (order_id, member_id, amount, status, payment_method, transaction_id)
        VALUES (#{orderId}, #{memberId}, #{amount}, #{status}, #{paymentMethod}, #{transactionId})
    </insert>

</mapper>
