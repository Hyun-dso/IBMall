<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itbank.mall.mapper.admin.AdminMessageMapper">

    <insert id="insertAdminMessage">
        INSERT INTO message (
            receiver_id,
            sender,
            title,
            content,
            image_url,
            is_read,
            created_at,
            scheduled_at
        ) VALUES (
            #{receiverId},
            'admin',
            #{title},
            #{content},
            #{imageUrl},
            0,
            NOW(),
            #{scheduledAt}
        )
    </insert>

</mapper>
