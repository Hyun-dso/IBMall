<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itbank.mall.repository.OrderRepository">

    <!-- ✅ 주문 상세 조회용 resultMap -->
    <resultMap id="OrderDetailResultMap" type="com.itbank.mall.dto.OrderDetailDto">
        <result property="orderId" column="order_id"/>
        <result property="memberId" column="member_id"/> <!-- 🔥 추가 -->
        <result property="orderDate" column="created_at"/>
        <result property="orderStatus" column="order_status"/>
        <result property="totalPrice" column="total_price"/>
        
        <result property="trackingNumber" column="tracking_number"/>
        <result property="deliveryStatus" column="delivery_status"/>
        <result property="recipient" column="recipient"/>
        <result property="address" column="address"/>
        <result property="phone" column="phone"/>

        <!-- 🔽 아이템 컬렉션 매핑 -->
        <collection property="items" ofType="com.itbank.mall.dto.OrderItemDto">
            <result property="productName" column="product_name"/>
            <result property="quantity" column="quantity"/>
            <result property="price" column="item_price"/>
        </collection>
    </resultMap>

    <!-- ✅ 주문 상세 조회 쿼리 -->
    <select id="findOrderDetailById" resultMap="OrderDetailResultMap" parameterType="long">
        SELECT
            o.order_id,
            o.member_id, <!-- 🔥 추가 -->
            o.created_at,
            o.status AS order_status,
            o.total_price,

            d.tracking_number,
            d.status AS delivery_status,
            d.recipient,
            d.address,
            d.phone,

            p.name AS product_name,
            oi.quantity,
            oi.price AS item_price

        FROM `order` o
        JOIN order_item oi ON o.order_id = oi.order_id
        JOIN product p ON oi.product_id = p.product_id
        LEFT JOIN delivery d ON o.order_id = d.order_id
        WHERE o.order_id = #{orderId}
    </select>

</mapper>
