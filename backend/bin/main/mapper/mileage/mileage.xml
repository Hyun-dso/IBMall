<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itbank.mall.mapper.mileage.MileageMapper">

    <!-- 마일리지 로그 저장 -->
    <insert id="insertMileageLog">
        INSERT INTO mileage_log (member_id, type, amount, description, created_at)
        VALUES (#{memberId}, #{type}, #{amount}, #{description}, #{createdAt})
    </insert>

    <!-- 마일리지 테이블에 해당 회원 존재 여부 확인 -->
    <select id="existsMileage" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM mileage
        WHERE member_id = #{memberId}
    </select>

    <!-- 기존 마일리지에 금액 추가 -->
    <update id="addMileage">
        UPDATE mileage
        SET total_mileage = total_mileage + #{amount},
            updated_at = NOW()
        WHERE member_id = #{memberId}
    </update>

    <!-- 마일리지 테이블에 최초 insert -->
    <insert id="insertInitialMileage">
        INSERT INTO mileage (member_id, total_mileage, updated_at)
        VALUES (#{memberId}, #{amount}, NOW())
    </insert>

</mapper>
