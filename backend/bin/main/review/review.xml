<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itbank.mall.mapper.review.ReviewMapper">

    <!-- 리뷰 저장 -->
    <insert id="insertReview" parameterType="com.itbank.mall.entity.review.Review">
        INSERT INTO review (
            product_id, member_id, payment_id, order_item_id,
            rating, content, image_url, likes,
            is_visible, is_private, created_at, updated_at
        ) VALUES (
            #{productId}, #{memberId}, #{paymentId}, #{orderItemId},
            #{rating}, #{content}, #{imageUrl}, #{likes},
            #{isVisible}, #{isPrivate}, NOW(), NOW()
        )
    </insert>

    <!-- 주문 상세 항목 기준으로 유저가 이미 작성한 리뷰 있는지 확인 -->
    <select id="findByMemberIdAndOrderItemId" resultType="com.itbank.mall.entity.review.Review">
        SELECT *
        FROM review
        WHERE member_id = #{memberId}
          AND order_item_id = #{orderItemId}
    </select>

    <!-- 특정 상품에 대한 리뷰 리스트 -->
    <select id="findByProductId" resultType="com.itbank.mall.entity.review.Review">
        SELECT *
        FROM review
        WHERE product_id = #{productId}
          AND is_visible = TRUE
        ORDER BY created_at DESC
    </select>

</mapper>
